extends layout

block content
	h1= title
	p Welcome to #{title} here is a change
	div (id"button" onClick="method()") Press Me
	var btnType = 'info'
	var btnSize = 'lg'
	button(type='button' class='btn btn-' + btnType + ' btn-' + btnSize)
	Button 1