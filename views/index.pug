extends layout

block content
	h1= title
	p Welcome to #{title} here is a change
	button(type='button', class='btn btn-' + btnType + ' btn-' + btnSize) Press Me
	<button type="button" class="btn btn-primary btn-lg" onclick="update()">Large button</button>
	div(id='target') Display

	script.
		const client_count = 0;
		function update(client_count) {
			client_count++;
			document.getElementById("target").innerHTML = "Count now 1";
		}